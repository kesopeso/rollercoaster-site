export enum NextEnvironment {
    DEVELOPMENT = 'development',
    STAGING = 'staging',
    PRODUCTION = 'production',
}

export enum Farm {
    ROLL = 'roll',
    ROLL_ETH = 'roll-eth',
}

export enum FarmToken {
    ROLL = 'ROLL',
    ROLL_ETH = 'ROLL-ETH',
}
